{
  "name": "union-arena",
  "private": true,
  "version": "0.0.0",
  "description": "Union Arena TCG — play online with real-time multiplayer and an automated rules engine.",
  "type": "module",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "dev": "pnpm -r --parallel dev"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
